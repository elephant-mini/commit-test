name: Github Actions Tests
on:
  [push, pull_request]
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - runs: echo "Running tests..."
      - runs: echo "${{ github.event_name }}"
      - runs: echo "Tests completed successfully!"
