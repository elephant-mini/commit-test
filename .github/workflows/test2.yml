name: Github Actions Tests2
on: [push, pull_request]
jobs:
  test2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.action }}"
      - run: echo "${{ github.ref }}"
      - run: echo "${{ github.repository }}"
      - run: echo "Tests completed successfully!"
